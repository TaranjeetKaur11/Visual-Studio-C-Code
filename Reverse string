public static void Main()
	{
		string userCommand = "";
		Console.WriteLine("Enter the string to reverse:");
	    userCommand = Console.ReadLine();
		Char[]arr = userCommand.ToCharArray();
		Array.Reverse(arr);
		for(int i=0; i<arr.Length;i++){
		Console.WriteLine(arr);
	    Console.ReadLine();
		}
